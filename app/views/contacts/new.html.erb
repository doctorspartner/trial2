<h1>New contact</h1>

<%if params[:company_id]%>
	<%= render 'form_company_contact' %>
<%else%>
	<%= render 'form_contact' %>
<%end%>

<%= link_to 'Back', contacts_path, :class => 'btn btn-mini btn-primary' %>
